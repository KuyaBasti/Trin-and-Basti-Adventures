{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "node -e 'let d=\"\";process.stdin.on(\"data\",c=>d+=c).on(\"end\",()=>{try{const cmd=(JSON.parse(d).tool_input||{}).command||\"\";if(/git commit/.test(cmd))console.log(JSON.stringify({hookSpecificOutput:{hookEventName:\"PostToolUse\",additionalContext:\"A git commit just ran. Run the docs pass (docs/README.md): add a dated entry to docs/05-progress.md now; update README.md and SYSTEM-DESIGN.md too if structure or status changed.\"}}))}catch(e){}})'",
            "timeout": 10,
            "statusMessage": "Checking docs pass"
          }
        ]
      }
    ]
  }
}
